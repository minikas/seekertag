# Asset provenance

All app footage is copied from the canonical September 20, 2026 recordings in `../edit/assets`. No app state was generated or recreated for this promotional cut. The owner is the Seeker session and the finder is the QEMU session.

| Asset | Source and usage |
| --- | --- |
| `my-items.mp4` | Current English Home; source 0–3.36 seconds at 0.8× |
| `unique-qr.mp4` | Travel Backpack tag; source 0–3.36 seconds at 0.8× |
| `chat-owner-receive.mp4` | Owner conversation; source 5.2–9.4 seconds |
| `chat-finder-receive.mp4` | Finder conversation; source 4.7–8.9 seconds |
| `reward-paid.mp4` | Same confirmed return; source 0–3.6 seconds |
| `logo.svg` | `apps/landing/public/favicon.svg`, SeekerTag brand artwork |
| `gsap.min.js` | Existing local runtime from `../edit/assets/gsap.min.js` |
| `Arimo.ttf` | [Google Fonts official repository](https://github.com/google/fonts/tree/main/ofl/arimo), SIL Open Font License included as `Arimo-OFL.txt` |
| `voice-*.wav`, `narration.wav` | Local Kokoro synthesis, `af_heart`, English text bundled alongside each line |
| `music.wav` | Original procedural composition generated by `author-audio.py`, seed 9121; no third-party samples |

The automatically adopted media inventory is in `.media/`. `narration-timing.json` records the exact spoken intervals. The rendered video uses the recordings' original colors, full phone screens and no artificial lens treatment.
